<button class="py-2 px-4 bg-secondary text-white font-semibold rounded-lg shadow-md hover:bg-primary focus:outline-none focus:ring-2 focus:ring-blue-400 focus:ring-opacity-75">
    {% if icon %}
        <i class="{{ icon }} mr-2"></i>
    {% endif %}
    {{ text }}
</button>