<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}mozoou.dev{% endblock %}</title>
    <link rel="icon"
          href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body class="bg-background">
{% block header %}
    {% for flash in app.flashes %}
        
    {% endfor %}
        <nav class="p-2 sm:px-4 md:rounded border-b-4 border-secondary" id="navbar-default">
            <div class="container flex items-center justify-between mx-auto px-3 text-title">
                <div>
                    <a href="/" class="flex items-center transition transform ease-in-out delay-150 hover:-translate-y-1 hover:scale-110 duration-300">
                        <img src="{{ asset('assets/img/light_logo.png') }}" class="h-16 mr-3 sm:h-16" alt="Mozoou Logo">
                        <span class="self-center text-xl font-semibold whitespace-nowrap">mozoou.dev</span>
                    </a>
                </div>
                <div>
                    <ul class="flex flex-col p-4 mt-4 space-y-1 md:flex-row md:space-x-4 md:mt-0 md:text-sm md:font-medium md:border-0 dark:border-gray-700">
                        <li>
                            <a href="{{ path('app_blog') }}" class="block py-2 px-3 my-1 hover:text-indigo-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 dark:hover:text-indigo-500">
                                Blog
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
    {% endblock header %}
    <main class="py-5">
        {% block body %}    
        {% endblock %}
    </main>
</body>
</html>
